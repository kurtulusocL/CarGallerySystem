@model CarGallerySystem.Entities.Models.Car
@{
    ViewBag.Title = "Kiralık - Satılık Araç Bilgisi Detayı";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid  dashboard-content">
    <div class="row">
        <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
            <div class="page-header">
                <h2 class="pageheader-title">@ViewBag.Title</h2>
                <div class="page-breadcrumb">
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="~/Admin/Index" class="breadcrumb-link">@Session["KullanıcıAdıSoyadı"]</a></li>
                            <li class="breadcrumb-item active" aria-current="page">@ViewBag.Title</li>
                        </ol>
                    </nav>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
            <div class="card">
                <h5 class="card-header">
                    <a href="~/Customer/Index" class="btn btn-primary btn-xs">Yeni Müşteri Listesi</a>
                </h5>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered first">
                            <thead>
                                <tr>
                                    <th>Rengi</th>
                                    <th>Kilometresi</th>
                                    <th>Toplam Araç Sayısı</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>@Model.Colour</td>
                                    <td>@Model.KM</td>
                                    <td>@Model.TotalCar</td>
                                </tr>
                            </tbody>
                            <tfoot>
                                <tr>
                                    <th>Rengi</th>
                                    <th>Kilometresi</th>
                                    <th>Toplam Araç Sayısı</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>