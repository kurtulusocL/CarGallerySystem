@model IEnumerable<CarGallerySystem.Entities.Models.Model>
@{
    Layout = null;
}

@foreach (var item in Model)
{
    <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 col-12">
        <div class="card">
            <div class="card-header d-flex">
                <h4 class="mb-0">@item.Name - @item.Brand.Name</h4>
            </div>
            <div class="card-body">               
                <p class="card-text">@item.Cars.Count() kayıtlı araba</p>
                <p class="card-text">@item.Customers.Count() kayıtlı müşteri</p>
                <a href="~/Model/Index" class="btn btn-primary">Model Listesi</a>
            </div>
        </div>
    </div>
}
